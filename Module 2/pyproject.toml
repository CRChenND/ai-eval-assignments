[project]
name = "module2-mars-hallucination"
version = "0.1.0"
description = "Module 2 evaluation pipeline for hallucination analysis with OpenRouter."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "matplotlib>=3.8,<4.0",
]

[tool.uv]
package = false
